let multiplayerPlay = (function(){

  // create new gon.jbuilder to rip data and setup board



  // if last move was opponent's, then enable click
  // $('#opponent-board .cell').on("click", clickCellCallback);

});
